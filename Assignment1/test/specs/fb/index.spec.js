const assert = require('assert') const b = browserconst FbLogin = require('../pages/FbLogin')const FbSearch = require('../pages/FbSearch')const conf = require('../wdio.conf.js')describe('User', ()=>{        it ('should login to FB with given credentials',()=> {        b.url(conf.config.baseUrl)        b.pause(3000)        FbLogin.mailid.setValue(conf.userDetails.email)        FbLogin.password.setValue(conf.userDetails.password)        FbLogin.loginBtn.click()      })      it ('should search for a friend',()=> {        b.pause(3000)        FbSearch.searchText.click()        FbSearch.enterText.setValue("Crist Cale")        FbSearch.searchBtn.click()        FbSearch.addBtn.click()      })      })